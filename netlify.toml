# 7Upload — static frontend with Netlify Functions backend

[build]
  publish = "public"
  command = "echo 'Building static site...'"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
